import React from "react";
import View from '../view/view';

const Task = ({className}) => {
    return (
        <li className={className}>
            <View />
        </li>
    );
}

export default Task;